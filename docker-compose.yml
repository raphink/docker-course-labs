version: '2'

services:
  product-service:
    build: ./python
    image: camptocamp/python_app:latest
    volumes:
      - QUOI:VERS?
    ports:
      - XXXX:XX
  website:
    build: ./website
    image: camptocamp/php_app:latest
    volumes:
      - .QUOI:VERS?
    ports:
      - XXXXX:XX
    depends_on:
      - XXXXXXXXXXXXXXXXX
